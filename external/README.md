# External source dependencies

Isaac-GR00T is intentionally kept as an external source dependency. Set
`ISAAC_GROOT_ROOT` or place it at `external/Isaac-GR00T`. The current machine's
sibling `/home/whf/Project/Isaac-GR00T` checkout is discovered automatically.

