# Optional room visual

The 168 MB `scene.glb` from the original development workspace is not required
for headless state-only smoke tests and was not copied into this repository.

For visual-policy rollouts, provide `scene/scene.glb` locally or pass an explicit
scene path to the runtime. Local GLB files in this directory are ignored by Git.

