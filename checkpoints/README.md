# External checkpoints

Model weights are intentionally not committed or copied into RExPolicy. Set
`GROOT_POLICY_CHECKPOINT` and `GROOT_VLM_MODEL`, or place compatible artifacts
under:

```text
checkpoints/groot/checkpoint-200000/
checkpoints/nvidia/Cosmos-Reason2-2B/
```

